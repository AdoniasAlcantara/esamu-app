<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textSize">@dimen/text_normal</item>
        <item name="android:windowActivityTransitions">true</item>
    </style>

    <!-- No action bar theme -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Full screen theme -->
    <style name="FullScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Drawer -->
    <style name="AppTheme.NavDrawer" parent="AppTheme.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Buttons -->
    <style name="ButtonNormal" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:height">@dimen/button_height</item>
        <item name="colorButtonNormal">#FC8236</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:drawablePadding">0dp</item>
    </style>

    <style name="ButtonAccent" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:height">@dimen/button_height</item>
        <item name="colorButtonNormal">#C63131</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:drawablePadding">0dp</item>
    </style>

    <!-- FAB -->
    <style name="FabLabel">
        <item name="android:background">@drawable/fab_label</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Text Input -->
    <style name="TextInput" parent="@style/AppTheme">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="colorError">@color/colorAccent</item>
        <item name="android:textSize">@dimen/text_normal</item>
    </style>
</resources>
